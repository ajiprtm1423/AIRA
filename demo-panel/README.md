# AIRAVAT Demo Panel

This folder contains a neutral, offline-only preview of the dashboard layout.

## Run locally

```bash
cd /workspaces/AIRA/demo-panel
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
